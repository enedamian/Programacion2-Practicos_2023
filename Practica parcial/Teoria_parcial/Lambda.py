sin_argumentos = lambda: "Hola, mundo"

cuadrado = lambda x: x ** 2

suma = lambda x, y: x + y

potencia = lambda base=2, exponente=3: base ** exponente

es_par = lambda x: True if x % 2 == 0 else False